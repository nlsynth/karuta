/* this modules adds +10 to data in address 0 */

def Kernel.main() {
  var a *int = 0;
  *a = *a + 10;
}

/* test it */
var p *int = 0;
*p = 10;
Kernel.main();
if (*p == 20) {
  print("maybe ok");
}
