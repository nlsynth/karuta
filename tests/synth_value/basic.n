// VERILOG_OUTPUT: a.v
def Kernel.main() {
  assert(true);
  assert(true && true);
  assert(true || false);
  assert(false || true);

  assert(1 + 1 == 2);
  assert(1 + 1 != 3);

  int #64 p;
  p = 65536 * 65536 + 65536;
  int s = p[31:0];
  assert(s == 65536);
}

main();

compile();
writeHdl("a.v");
