// VERILOG_OUTPUT: a.v

// Synth failure for an inifinite loop.

channel Kernel.c int

func main() {
  while (true) {
    c.write(10)
    print(c.read())
  }
}

compile();
// Unused. Just to see if this doesn't fail.
writeHdl("a.v")
