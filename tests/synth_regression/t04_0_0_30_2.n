// VERILOG_OUTPUT: a.v

// yet another fix

int Kernel.MT[1024] = {0};

def main() {
  int i;
  i = 1;
  int prev = MT[0];
  for (i = 1; i < 2; ++i) {
    int cur;
    cur = (prev >> 30) + 1;
    MT[i] = cur[31:0];
  }
  assert(MT[1] == 1);
//  print(MT[1]);
}

compile();
write_hdl("a.v");

main();
