// VERILOG_OUTPUT: a.v

// fix for memory access reordering.

int Kernel.MT[1024] = {0};

def main() {
  MT[0] = 0;
  int prev = MT[0];
  prev = prev + 1;
  MT[1] = prev;
  assert(MT[1] == 1);
}

compile();
write_hdl("a.v");

// main should run later not to write ram.
main();
