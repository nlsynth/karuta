// VERILOG_OUTPUT: a.v
def Kernel.g(int x) {
  return x;
}

def Kernel.f(int x, int y) {
  return 10 + g(x);
}

def Kernel.main() {
  int x;
  x = 65535;
  if (x < 10) {
    x = 0xff & x;
  }
  x = f(x+1, 10);
  assert(x == 65546);
}

Kernel.compile();
Kernel.writeHdl("a.v");
