#! /bin/sh

# Neon light compiler frontend wrapper to be installed.
# (the original file is nli.in and nli is generated by configure script)

prefix=@prefix@

export NLI_DIR=@datadir@/nli
PATH=@prefix@/bin:$PATH

if test "${GDB+set}" = set; then
  GDB="$GDB --args"
fi

$GDB nli-bin $@
